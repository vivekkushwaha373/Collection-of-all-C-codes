#include<iostream>
using namespace std;
int main()
{
    int a=9,b=8;
    cout<<(a!=b)<<"and "<<(b==0);
    cout<<(a+b)<<"is the answer of (a+b)"<<endl;
    cout<<(a-b)<<"is the answer of (a-b)"<<endl;
    cout<<(a/b)<<"is the answer of (a/b)"<<endl;
    cout<<(a*b)<<"is the answer of (a*b)"<<endl;
    cout<<(a%b)<<"is the answer of (a%b)"<<endl;
    return 0;
}